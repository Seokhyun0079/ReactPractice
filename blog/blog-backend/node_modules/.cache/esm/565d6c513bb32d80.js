let jwt;_3bb‍.x([["default",()=>_3bb‍.o]]);_3bb‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) =>{
    const token = ctx.cookies.get('access_token');
    if(!token) return next();
    try{
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id : decoded._id,
            username : decoded.username,
        };
        _3bb‍.g.console.log(decoded);
        return next();
    }catch(e){
        return next();
    }
};

_3bb‍.d(jwtMiddleware);